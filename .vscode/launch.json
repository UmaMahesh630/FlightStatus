{
  "version": "0.2.0",
  "configurations": [
    {
      "name": ".NET Core Attach (Standard)",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:pickProcess}"
    }
  ]
}
